import time


def grade_exam(exam_scores):
    grade_dict = {}

    # TODO: Calculate the grade distribution for the exam scores
    # Hint: Use a loop to iterate through the exam_scores list, and use conditional statements to classify each score into a grade category

    grade_dict["date"] = time.strftime("%m/%d/%Y")

    return grade_dict
